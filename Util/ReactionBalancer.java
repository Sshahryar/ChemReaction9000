package Util;

public class ReactionBalancer {
    public static String balance(String reaction) {
        //This is where the reactants and products get split
        String reactantsStr = parts [0].trim();
        String productsStr = parts [1].trim();

        return reaction;
    }
}
